<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 课表数据要从教务系统（https）实时拉取。
         注意：debug/profile 的 Manifest 模板里自带这条权限，main 里没有，
         所以 release 包必须在这里显式声明，否则装上后网络请求全部失败。 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 上课提醒：Android 13+ 发通知要用户授权（运行时申请，见 class_notifier_io.dart）。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 上课提醒要「准点响」，得用精确闹钟。用户在系统设置里不放行时，
         客户端会退化成不精确排期（可能晚几分钟），而不是干脆不提醒。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- 重启后已排的提醒要重新登记，否则关机一次就全丢了。 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- 「保活」指引：弹系统的「允许忽略电池优化」确认框需要这条权限
         （只是让系统弹框，是否放行仍由用户决定）。 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:label="广应科课表"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 「下一节课」桌面小组件（数据由 App 内算好推过来，见 NextClassWidgetProvider）。
             exported=false：只接收系统广播与自家闹钟，不对外开放。 -->
        <receiver
            android:exported="false"
            android:name=".NextClassWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_class_widget_info"/>
        </receiver>

        <!-- 2×2 尺寸 —— 信息展示的主要参考版式（与大尺寸共用渲染分支）。 -->
        <receiver
            android:exported="false"
            android:name=".NextClassWidgetSquareProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_class_widget_square_info"/>
        </receiver>

        <!-- 同一份数据的紧凑尺寸（2 格宽 × 1 格高）：两行，只显示课程名与时间/地点。
             刷新逻辑与大尺寸共用（见 NextClassWidgets），App 推一次数据三种尺寸同时更新。 -->
        <receiver
            android:exported="false"
            android:name=".NextClassWidgetSmallProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_class_widget_small_info"/>
        </receiver>

        <!-- 同一份数据的窄竖条尺寸（1 格宽 × 2 格高）：一列，状态/课程/时间/地点。
             刷新逻辑与大尺寸共用（见 NextClassWidgets）。 -->
        <receiver
            android:exported="false"
            android:name=".NextClassWidgetTallProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_class_widget_tall_info"/>
        </receiver>

        <!-- flutter_local_notifications 的两个接收器（插件文档要求显式声明，否则
             定时提醒不会响）。exported=false：只接收系统广播，不对外开放。 -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
